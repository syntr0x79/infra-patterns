# Values injected by CI. In production these come from the environment or a
# secret store; here they are placeholders that only need to be syntactically
# plausible, because the job checks syntax, not connectivity.
DB_NODE1_IP=10.0.0.200
DB_NODE2_IP=10.0.0.201
CACHE_NODE1_IP=10.0.0.210
CACHE_NODE2_IP=10.0.0.211
CACHE_PASSWORD=ci-dummy
