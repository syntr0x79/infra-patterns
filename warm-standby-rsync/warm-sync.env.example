# /etc/warm-sync.env on the primary
STANDBY_HOST=standby.internal
STANDBY_USER=standby
SSH_KEY=/root/.ssh/id_warm_sync
SYNC_ROOT=/opt
SERVICES=api,worker,web
# Live SQLite databases needing a consistent snapshot rather than a file copy
SQLITE_FILES=/opt/api/state.db
